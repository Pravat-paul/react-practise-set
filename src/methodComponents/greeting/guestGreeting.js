import React from "react";

function GuestGreeting(props) {
    return (
        <div className="message">
            <p>Greeting app based on login or logout using method components!</p>
            <h4>Please login!</h4>
        </div>
    );
}

export default GuestGreeting;